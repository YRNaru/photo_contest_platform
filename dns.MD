# `vrchat-photocontest-platform.site` DNS調査結果

## 基本情報

| 項目 | 値 |
|------|-----|
| 調査対象ドメイン | `vrchat-photocontest-platform.site` |
| 使用DNSサーバー | `10.255.255.254`（ポート53） |

---

## レコード別結果

### ✅ A レコード（IPv4アドレス）
| 項目 | 値 |
|------|-----|
| IPアドレス | `162.43.32.102` |
| 回答種別 | Non-authoritative（キャッシュ応答） |

> IPv4での名前解決は**正常に成功**しています。

---

### ❌ AAAA レコード（IPv6アドレス）
- 結果：**レコードなし**
- IPv6には**対応していません**。

---

### ✅ NS レコード（ネームサーバー）
| # | ネームサーバー |
|---|----------------|
| 1 | `ns1.xdomain.ne.jp` |
| 2 | `ns2.xdomain.ne.jp` |
| 3 | `ns3.xdomain.ne.jp` |

> **xdomain.ne.jp**（エックスドメイン）がDNSを管理しています。

---

### ❌ MX レコード（メールサーバー）
- 結果：**レコードなし**
- このドメインでのメール受信は**設定されていません**。

---

### ❌ CNAME レコード（エイリアス）
- 結果：**レコードなし**
- 別名（エイリアス）は**設定されていません**。

---

## SOA情報（権威情報）

MX・CNAMEの問い合わせ時に返却されたSOAレコードです。

| 項目 | 値 | 説明 |
|------|----|------|
| origin | `ns1.xdomain.ne.jp` | プライマリDNSサーバー |
| mail addr | `root.xdomain.ne.jp` | 管理者メールアドレス |
| serial | `0` | ゾーンのシリアル番号 |
| refresh | `10800`秒（3時間） | セカンダリが更新を確認する間隔 |
| retry | `3600`秒（1時間） | 更新失敗時の再試行間隔 |
| expire | `604800`秒（7日間） | ゾーン情報の有効期限 |
| minimum TTL | `3600`秒（1時間） | ネガティブキャッシュの最小TTL |

---

## まとめ

| レコード | 状態 | 備考 |
|----------|------|------|
| A (IPv4) | ✅ 設定あり | `162.43.32.102` |
| AAAA (IPv6) | ❌ 未設定 | IPv6非対応 |
| NS | ✅ 設定あり | xdomain管理（3台） |
| MX (メール) | ❌ 未設定 | メール受信不可 |
| CNAME | ❌ 未設定 | エイリアスなし |