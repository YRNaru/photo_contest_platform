# Chrome Extension

`chrome://extensions` で「デベロッパーモード」を有効にし、この `chrome-extension/` ディレクトリを `Load unpacked` で読み込んでください。

初回は X のツイート詳細ページで「この作品を応募」を押し、`Backend URL` に API サーバーのベース URL 例: `http://localhost:18001` を入力してからログインします。
